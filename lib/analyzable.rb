module Analyzable
  
  def average_price(products)
    #all_products.each do |price|
    #price += price
    #sum_of_prices / all_products.count
  end
  
  def print_report
      #self.print.to_s
      #CSV formatting 
  end
  
  def count_by_brand
      #hash
      #all_products.each do |brand|
      #brands_hash = {brand, count}
  end
  
  def count_by_name
      #hash
      #all_products.each do |name|
      #names_hash = {name, count} 
  end
end
